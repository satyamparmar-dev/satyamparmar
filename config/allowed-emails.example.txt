# This repo tracks config/allowed-emails.txt in Git. For a new project, copy this to
# allowed-emails.txt, add one email per line, then run: npm run build:allowlist
#
# you@example.com
# teammate@company.org
