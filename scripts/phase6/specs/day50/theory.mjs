export const theoryTitle = "REST Best Practices and OpenAPI";

export const theoryBase = "### Day 50 — REST best practices with OpenAPI (deep dive)\n\nThis day extends Day 49's HTTP semantics with the **contract layer**: how teams document, lint, evolve, and safely publish APIs using **OpenAPI 3.x** alongside **Java/Spring** — without treating documentation as an afterthought.\n\n---\n\n### 1. Formal definition — what OpenAPI specifies\n\n**OpenAPI Specification (OAS)** is a machine-readable description of an HTTP API: **paths**, **operations**, **parameters**, **request bodies**, **responses**, **components** (reusable schemas, security schemes, headers), and **metadata** (`info`, `servers`, `tags`). It is a **contract** for humans, gateways, mock servers, **SDK generators**, and **contract tests**. OAS 3.x uses a **JSON Schema dialect** for `schema` objects with OpenAPI-specific extensions (e.g. `nullable`, `discriminator`). **Swagger** historically named the ecosystem; today **Swagger UI** renders OAS, while **springdoc-openapi** emits OAS from Spring MVC/WebFlux.\n\n**Interview angle:** distinguish **spec** (the YAML/JSON artefact) from **UI** (Swagger UI) and from **runtime security** — the UI must not become the only access control.\n\n---\n\n### 2. Internals and mechanism — code-first vs contract-first\n\n**Code-first (Spring default path):** Controllers and DTOs are authoritative; **springdoc** introspects annotations (`@Operation`, `@Schema`, `@Parameter`) and emits `/v3/api-docs`. Fast for teams already on Spring; risk is **spec drift** if annotations are incomplete or if someone bypasses documented validation at runtime.\n\n**Contract-first:** `openapi.yaml` lives in git; **openapi-generator** produces interfaces/DTOs; implementors fill in. Strong for **partner APIs** and multi-language consumers; higher upfront rigour.\n\n**Hybrid (common at scale):** code-first emission + **CI** compares output to a **baseline** artifact from `main`, or applies **Spectral** rules so every PR fails on missing `operationId`, undocumented `4xx`, or unbounded arrays.\n\n---\n\n### 3. Most-probed interview angles — breaking vs non-breaking changes\n\n| Change | Typical impact | Notes |\n|--------|----------------|-------|\n| Add optional response field | Usually **non-breaking** | Postel's law — tolerant readers ignore unknown properties |\n| Add **required** response field | Often **breaking** | Strict clients and codegen fail deserialization |\n| Remove / rename field | **Breaking** | Needs version bump + deprecation window |\n| Narrow enum (remove value) | **Breaking** | Exhaustive `switch` in clients breaks |\n| Widen enum (add value) | Often **non-breaking** | Clients must tolerate unknown enum labels or use strings |\n| Change `operationId` | **Breaks codegen** | Feign / OpenAPI Generator method names change |\n| Tighten security (new scope) | **Breaking** for some callers | Tokens without scope get **403** at gateway |\n\n**Interview angle:** tie breaking-change policy to **CI** — not to memory. Mention **openapi-diff** or vendor tools (**Optic**, **Bump.sh**) plus **consumer Pact**.\n\n---\n\n### 4. Practical mapping — Java/Spring + springdoc\n\nUse **`@Tag`**, **`@Operation(summary, operationId)`**, **`@ApiResponse`** / **`@Schema`** on DTOs, **`GroupedOpenApi`** beans to split **public vs internal** surfaces, and **`springdoc.swagger-ui.enabled=false`** in production unless behind **SSO**. Prefer **`ProblemDetail`** (Spring 6) and document error bodies under **`components.responses`**. For pagination, mirror actual query params (`cursor`, `limit`) in the spec with `style`/`explode` where relevant.\n\n**Interview angle:** show you know **`springdoc-openapi-starter-webmvc-ui`** vs **WebFlux** starter, and that **WebTestClient** + **springdoc** can snapshot the emitted JSON in tests.\n\n---\n\n### 5. Documentation quality — examples, servers, and extensions\n\nPopulate **`examples`** on schemas and parameters — reduces integration time more than prose. Declare **`servers`** per environment; avoid hard-coding prod URLs only. Use **`x-` extensions** sparingly and **namespaced** (`x-acme-correlation`) so tooling ignores unknown vendor fields without collision. Keep **`tags`** aligned with bounded contexts for readable Swagger UI navigation.\n\n---\n\n### 6. Security and compliance — what goes wrong in production\n\nExposing **Swagger UI** on the same host without **network policy** or **auth** invites **credential stuffing** via \"Try it out\", leaks **internal operation** names, and complicates **PCI** scope. Prefer **separate internal docs host**, **IP allow lists**, or **OAuth2**-protected doc portals. Never rely on \"security through obscurity\" of undocumented paths — scanners exist.\n\n---\n\n### 7. Toolchain — Spectral, codegen, and CI gates\n\n**Spectral** applies style rules: require `operationId`, ban `200` without `description`, enforce `info.contact`. **openapi-generator** produces Java/Kotlin clients — pin generator version and regenerate in CI. Combine **provider-driven** verification with **Pact** so consumers' expectations fail the build when response shapes regress.\n\n---\n\n### 8. Story you can tell in 60 seconds\n\n**Symptom:** Mobile release broke after a \"doc-only\" PR renamed **`operationId`** and regenerated the Android SDK — runtime paths were unchanged but **method names** shifted. **Root cause:** no CI check on **generator diff**; **operationId** treated as cosmetic. **Mitigation:** stable **`operationId`** policy, **`x-codegen-operation-id`**, or pinned **SDK** major with **changelog** gate; add CI step that fails if **breaking** OpenAPI diff is not labelled with **semver** bump. Shows you connect **documentation** to **release engineering**.";
