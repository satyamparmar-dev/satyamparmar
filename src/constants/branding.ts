/**
 * Full app / project display name (UI, HTML title, curriculum.json, prompts).
 * Deploy uses Vite `base: './'` so GitHub Pages works for any repository name; npm package is `satyverse-satyam-parmar`.
 */
export const APP_DISPLAY_NAME = 'Satyverse(Satyam Parmar)';
