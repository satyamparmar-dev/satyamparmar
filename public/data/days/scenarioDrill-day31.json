{
  "day": 31,
  "title": "Optional and Modern Java APIs",
  "phaseId": "phase4",
  "tags": [
    "Optional",
    "orElseGet",
    "API design"
  ],
  "scenarios": [
    {
      "id": "d31-s1",
      "question": "`optional.orElse(expensive())` always allocates—fix?",
      "signals": [
        "orElseGet",
        "lazy"
      ],
      "answer": "Use **`orElseGet(() -> expensive())`**—supplier runs **only if empty**.",
      "followUps": [
        {
          "question": "Field type Optional?",
          "answer": "**Avoid**—`Optional` not designed for fields/serialization; use for **return types**."
        }
      ]
    },
    {
      "id": "d31-s2",
      "question": "`Optional.get()` in code review—comment?",
      "signals": [
        "orElseThrow",
        "NoSuchElement"
      ],
      "answer": "Prefer **`orElseThrow()`** with message supplier or **`orElse`**—**`get()`** throws bare **NoSuchElementException**—poor diagnostics.",
      "followUps": [
        {
          "question": "flatMap chaining?",
          "answer": "Avoid **nested Optional**—**`flatMap`** flattens **Optional<Optional<T>>** patterns."
        }
      ]
    },
    {
      "id": "d31-s3",
      "question": "`String.lines()` vs `split(\"\\\\n\")`—difference?",
      "signals": [
        "lazy stream",
        "line terminators"
      ],
      "answer": "`lines()` handles **\\n, \\r, \\r\\n** per spec and returns **Stream**—memory friendly for big strings.",
      "followUps": [
        {
          "question": "`String.isBlank` vs `isEmpty`?",
          "answer": "`isBlank` true for **whitespace-only**—validation UX difference."
        }
      ]
    }
  ]
}
