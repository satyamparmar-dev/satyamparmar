{
  "day": 38,
  "title": "Spring Core - IoC & DI",
  "phaseId": "phase5",
  "tags": [
    "Bean",
    "ApplicationContext",
    "constructor injection"
  ],
  "scenarios": [
    {
      "id": "d38-s1",
      "question": "Why prefer constructor injection over field injection in Spring?",
      "signals": [
        "immutability",
        "testability",
        "required deps"
      ],
      "answer": "**Immutable** after construct, **explicit** required dependencies, easier **unit tests** without reflection hacks, avoids **circular** dependency surprises earlier.",
      "followUps": [
        {
          "question": "Optional dependency?",
          "answer": "**`@Autowired(required=false)`** or **`Optional<T>`** ctor param—document clearly."
        }
      ]
    },
    {
      "id": "d38-s2",
      "question": "Two beans same interface—how to inject specific one?",
      "signals": [
        "@Qualifier",
        "@Primary",
        "name"
      ],
      "answer": "**`@Qualifier(\"beanName\")`**, or **`@Primary`** for default, or **`@Bean` method param** name matching.",
      "followUps": [
        {
          "question": "What is `@Configuration` proxy?",
          "answer": "**CGLIB** proxies **`@Bean` methods** to enforce **singleton** semantics within config—don’t call `@Bean` methods directly from Java without going through proxy."
        }
      ]
    },
    {
      "id": "d38-s3",
      "question": "`ApplicationContext` vs `BeanFactory` in interview?",
      "signals": [
        "eager",
        "events",
        "i18n"
      ],
      "answer": "`ApplicationContext` is **superset**—**eager** init of singletons by default, **events**, **resource** handling—what Spring Boot uses.",
      "followUps": [
        {
          "question": "Component scanning performance?",
          "answer": "Large classpath scans cost startup—**explicit** packages, **Spring Native**/AOT different story."
        }
      ]
    }
  ]
}
