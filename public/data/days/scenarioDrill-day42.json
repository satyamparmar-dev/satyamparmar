{
  "day": 42,
  "title": "Spring Boot Internals",
  "phaseId": "phase5",
  "tags": [
    "auto-configuration",
    "starter",
    "conditional"
  ],
  "scenarios": [
    {
      "id": "d42-s1",
      "question": "How does `@SpringBootApplication` relate to auto-configuration?",
      "signals": [
        "@EnableAutoConfiguration",
        "META-INF/spring.factories",
        "imports"
      ],
      "answer": "Composes **`@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`**—**auto-config** loads **conditional** beans based on **classpath** and **properties**.",
      "followUps": [
        {
          "question": "Exclude auto-config?",
          "answer": "`@SpringBootApplication(exclude=...)` or **`spring.autoconfigure.exclude`** property."
        }
      ]
    },
    {
      "id": "d42-s2",
      "question": "Bean missing at runtime only in prod profile—debug steps?",
      "signals": [
        "conditional",
        "classpath",
        "property"
      ],
      "answer": "Check **`@ConditionalOn*`** annotations, **profile-specific** configs, **missing** dependency in prod **fat jar**, **`debug=true`** report.",
      "followUps": [
        {
          "question": "`spring.factories` vs `imports` file?",
          "answer": "Boot 3+ shifts toward **`AutoConfiguration.imports`**—know migration basics."
        }
      ]
    },
    {
      "id": "d42-s3",
      "question": "Fat jar vs thin jar trade-off?",
      "signals": [
        "deployment",
        "layers"
      ],
      "answer": "**Fat** self-contained vs **image** size; **layered jars** for **Docker** cache efficiency.",
      "followUps": [
        {
          "question": "DevTools restart?",
          "answer": "**Classloader** restart fast path—**never** in prod."
        }
      ]
    }
  ]
}
