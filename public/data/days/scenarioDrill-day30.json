{
  "day": 30,
  "title": "Streams API",
  "phaseId": "phase4",
  "tags": [
    "lazy",
    "terminal",
    "parallel"
  ],
  "scenarios": [
    {
      "id": "d30-s1",
      "question": "Stream pipeline has no output—only `filter` and `map`—bug?",
      "signals": [
        "lazy",
        "terminal operation"
      ],
      "answer": "Intermediate ops are **lazy**—nothing runs until **terminal** (`collect`, `forEach`, `reduce`).\n\nAdd **`toList()`** or **`collect(Collectors.toList())`**.",
      "followUps": [
        {
          "question": "Side effects in forEach?",
          "answer": "**Discouraged**—ordering with **parallel** undefined; prefer **`collect`** into structures."
        }
      ]
    },
    {
      "id": "d30-s2",
      "question": "When is `parallelStream()` a bad idea in a Spring service?",
      "signals": [
        "fork join",
        "pool",
        "small data"
      ],
      "answer": "**Small** collections → overhead dominates. Uses **common pool**—can **starve** other work; **blocking** inside tasks **bad**.\n\nPrefer **explicit** executor control for CPU work at scale.",
      "followUps": [
        {
          "question": "Stateful lambda hazard?",
          "answer": "**Race conditions**—streams should be **stateless** side-effect free for correctness."
        }
      ]
    },
    {
      "id": "d30-s3",
      "question": "`findFirst` vs `findAny` in parallel stream?",
      "signals": [
        "ordering",
        "performance"
      ],
      "answer": "**`findFirst`** may need **more coordination** to preserve encounter order. **`findAny`** cheaper in parallel when order irrelevant.",
      "followUps": [
        {
          "question": "Short-circuit ops?",
          "answer": "**`anyMatch`** stops early—good for **large** sources when predicate rare."
        }
      ]
    },
    {
      "id": "d30-s4",
      "question": "Given `List<Order>`, produce `Map<String, Long>` = count of orders **per customerId** using Streams.",
      "signals": [
        "groupingBy",
        "counting"
      ],
      "answer": "`orders.stream().collect(Collectors.groupingBy(Order::getCustomerId, Collectors.counting()))`.",
      "followUps": [
        {
          "question": "Sum order totals per customer instead?",
          "answer": "Same `groupingBy` with **`Collectors.summingInt(Order::getTotal)`** (or `BigDecimal` reduce downstream)."
        }
      ]
    },
    {
      "id": "d30-s5",
      "question": "Split a list of integers into **evens** and **odds** with one collector.",
      "signals": [
        "partitioningBy",
        "Predicate"
      ],
      "answer": "`stream.collect(Collectors.partitioningBy(n -> n % 2 == 0))` → **`Map<Boolean, List<Integer>>`**.",
      "followUps": [
        {
          "question": "More than two buckets?",
          "answer": "Use **`groupingBy`** with a function returning an enum or string key — not partitioningBy."
        }
      ]
    },
    {
      "id": "d30-s6",
      "question": "`Collectors.toMap(keyMapper, valueMapper)` throws on duplicate keys — fix?",
      "signals": [
        "merge function",
        "BinaryOperator"
      ],
      "answer": "Add **merge**: `Collectors.toMap(K::id, v -> v, (a,b) -> b)` for last-wins, or **`groupingBy`** if one-to-many.",
      "followUps": [
        {
          "question": "Concurrent map?",
          "answer": "**`Collectors.toConcurrentMap`** when safe and parallel stream benefits."
        }
      ]
    },
    {
      "id": "d30-s7",
      "question": "What does `distinct()` use to decide uniqueness for a stream of custom `Employee` objects?",
      "signals": [
        "equals",
        "hashCode"
      ],
      "answer": "**`equals` / `hashCode`** of each element — default **reference** equality if not overridden (surprise duplicates).",
      "followUps": [
        {
          "question": "Distinct by email only?",
          "answer": "Use **`filter` with `seen` Set**, or map to key then **`distinct`** on keys, or **`Collectors.toMap`** by key."
        }
      ]
    },
    {
      "id": "d30-s8",
      "question": "When is `flatMap` the right choice vs `map`?",
      "signals": [
        "one-to-many",
        "nested collections"
      ],
      "answer": "**`map`** is **1:1** element → element. **`flatMap`** when each element expands to **many** (e.g. line → words, order → line items).",
      "followUps": [
        {
          "question": "`Optional` in pipeline?",
          "answer": "**`flatMap(Optional::stream)`** (Java 9+) or filter-map-to-one to avoid **`Optional<Optional<T>>`**."
        }
      ]
    },
    {
      "id": "d30-s9",
      "question": "Why can `peek` be dangerous for required business logic?",
      "signals": [
        "side effects",
        "optimization"
      ],
      "answer": "It’s for **debugging**; the API does not guarantee **every** element is seen or **order** in all cases—**don’t** rely on peek for correctness.",
      "followUps": [
        {
          "question": "Better pattern for logging each stage?",
          "answer": "**Structured logging** outside stream, or **`map` returning same value** (still side effect—prefer explicit loop if complex)."
        }
      ]
    }
  ]
}
