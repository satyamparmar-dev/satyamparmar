{
  "day": 5,
  "title": "Strings and String Methods",
  "phaseId": "phase1",
  "tags": [
    "String",
    "immutability",
    "StringBuilder"
  ],
  "scenarios": [
    {
      "id": "d5-s1",
      "question": "Profiling shows huge `String` allocation from `+` inside a loop. Fix?",
      "signals": [
        "immutable String",
        "StringBuilder",
        "StringBuffer"
      ],
      "answer": "## In simple terms\nEach `+` may create **new String objects** because **`String` is immutable**. In a tight loop that is **garbage** and slow.\n\n## Fix\n```java\nStringBuilder sb = new StringBuilder(estimatedSize);\nfor (var item : items) {\n    sb.append(item).append(',');\n}\nreturn sb.toString();\n```\nUse **`StringBuffer`** only if you truly need **synchronized** appends (rare).\n\n## Extra\nJava may optimize **constant** concatenation at compile time—problem is **dynamic** loops.",
      "followUps": [
        {
          "question": "Is string concatenation with `+` ever fine?",
          "answer": "**Yes** for a **few** pieces or when the compiler can fold **static** parts. For **many** dynamic pieces in a loop, use **`StringBuilder`**."
        },
        {
          "question": "What does `intern()` do and when to avoid it?",
          "answer": "Puts a copy in the **string pool**. Abuse can **bloat memory** and is rarely needed; prefer **dedup maps** with bounded size for business keys."
        },
        {
          "question": "`strip()` vs `trim()`?",
          "answer": "`strip()` (Java 11+) removes **Unicode whitespace**; `trim()` removes **≤ U+0020** space chars. Prefer **`strip`** for international text."
        }
      ]
    },
    {
      "id": "d5-s2",
      "question": "Comparing usernames with `equalsIgnoreCase` for login—what can go wrong?",
      "signals": [
        "locale rules",
        "Turkish I",
        "security",
        "normalize"
      ],
      "answer": "## Risk\n**Locale-sensitive** case mapping can surprise you (classic **Turkish locale** `I` issues). For **security-sensitive** identifiers, use **`Locale.ROOT`** or normalize explicitly.\n\n## Safer pattern\n```java\na.equalsIgnoreCase(b) // still locale-insensitive for ASCII\n// or\na.toLowerCase(Locale.ROOT).equals(b.toLowerCase(Locale.ROOT))\n```\nFor natural language, consider **`Collator`**.\n\n## Also\n**Trim** input, consider **Unicode normalization (NFC)** for international usernames if policy allows.",
      "followUps": [
        {
          "question": "Why is `==` wrong for strings?",
          "answer": "`==` checks **same object in memory**, not same characters. Two strings with identical text can be **different objects** unless interned."
        },
        {
          "question": "What is `contentEquals` for?",
          "answer": "Compare `String` to **`StringBuilder` / `CharSequence`** without building another String—can save allocation."
        }
      ]
    },
    {
      "id": "d5-s3",
      "question": "Regex on user input in a hot path is slow. What do you check first?",
      "signals": [
        "Pattern.compile once",
        "catastrophic backtracking",
        "Possessive quantifiers"
      ],
      "answer": "## Quick wins\n1. **Compile once**: `private static final Pattern P = Pattern.compile(...)`—not inside the loop.\n2. Review regex for **catastrophic backtracking** on malicious input.\n3. Consider **simple string ops** (`indexOf`, `regionMatches`) if you do not need full regex power.\n\n## Interview depth\nMention **RE2**-style libraries only if relevant; standard JDK `Pattern` is what most interviews expect.",
      "followUps": [
        {
          "question": "What is `Matcher.quoteReplacement` for?",
          "answer": "When using **`replaceAll`**, dollar signs and backslashes in **replacement** strings are special—`quoteReplacement` escapes them safely."
        }
      ]
    }
  ]
}
