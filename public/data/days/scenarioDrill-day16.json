{
  "day": 16,
  "title": "Comparable and Comparator",
  "phaseId": "phase2",
  "tags": [
    "natural order",
    "sort",
    "stable sort"
  ],
  "scenarios": [
    {
      "id": "d16-s1",
      "question": "`Collections.sort(list)` throws `ClassCastException` at runtime—class implements no `Comparable`. Prevention?",
      "signals": [
        "generics",
        "Comparator",
        "API design"
      ],
      "answer": "## Cause\n`sort` needs **natural order** via **`Comparable`** or an explicit **`Comparator`**.\n\n## Fixes\n1. Implement **`Comparable<T>`** if **one canonical order** exists (document **consistency with equals**).\n2. Pass **`Comparator.comparing(...)`** for **context-specific** order (e.g. by `createdAt`).\n3. Use **generic** APIs so **compile-time** catches wrong types where possible.\n\n## Interview tip\n**`compareTo` contract**: sign only matters; return 0 for **equal for ordering**—not always same as **`equals`** unless you enforce **consistency**.",
      "followUps": [
        {
          "question": "What breaks if compareTo inconsistent with equals?",
          "answer": "**SortedSet/SortedMap** behave oddly—**“equal”** per `compareTo==0` may collapse entries differently than `equals`."
        },
        {
          "question": "Nulls in sort?",
          "answer": "Use **`Comparator.nullsFirst/Last`**—explicit policy beats `NullPointerException`."
        }
      ]
    },
    {
      "id": "d16-s2",
      "question": "Sort users by department, then salary descending, then name. How in Java?",
      "signals": [
        "thenComparing",
        "reversed",
        "key extractors"
      ],
      "answer": "## Idiomatic Java 8+\n```java\nusers.sort(Comparator\n    .comparing(User::getDepartment)\n    .thenComparing(User::getSalary, Comparator.reverseOrder())\n    .thenComparing(User::getName));\n```\n\n## Interview tip\nMention **stable sort** (`TimSort`)—equal keys keep **relative order**—useful in **multi-key** sorts.",
      "followUps": [
        {
          "question": "Performance of comparing extracted keys?",
          "answer": "Fine for interviews; at extreme scale avoid **repeated** extraction with **key caching** or **precomputed** sort keys."
        },
        {
          "question": "Locale-sensitive name sort?",
          "answer": "Use **`Collator`**—ASCII sort is wrong for **internationalized** names."
        }
      ]
    },
    {
      "id": "d16-s3",
      "question": "PR uses `a.getId() - b.getId()` in `compare` for integers—risk?",
      "signals": [
        "overflow",
        "Integer.compare",
        "Comparator.comparingInt"
      ],
      "answer": "## Bug\nSubtraction **overflows** for large ints—ordering becomes **wrong**.\n\n## Fix\n**`Integer.compare(x,y)`** or **`Comparator.comparingInt(User::getId)`**.\n\n## Interview line\n“Never subtract for `compare`—use **`compare` helpers**.”",
      "followUps": [
        {
          "question": "What about long ids?",
          "answer": "**`Long.compare`**—same overflow issue in theory if you tried subtraction with `long` in tricky expressions; use API."
        }
      ]
    }
  ]
}
