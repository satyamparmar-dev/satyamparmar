{
  "day": 60,
  "title": "Kafka Producers Deep Dive",
  "phaseId": "phase7",
  "tags": [
    "acks",
    "idempotence",
    "batching"
  ],
  "scenarios": [
    {
      "id": "d60-s1",
      "question": "`acks=all` still lost data—possible causes?",
      "signals": [
        "min.insync.replicas",
        "unclean",
        "producer config"
      ],
      "answer": "If **`min.insync.replicas`** too low or **unclean** election, **leader** ack may not mean **durable** on enough brokers—tune **ISR** + **producer retries** + **idempotence**.",
      "followUps": [
        {
          "question": "Producer idempotence scope?",
          "answer": "Per **producer session** with **PID**—still need **correct** **`max.in.flight`** with idempotence enabled per supported matrix."
        }
      ]
    },
    {
      "id": "d60-s2",
      "question": "Latency vs throughput tuning for producer?",
      "signals": [
        "linger.ms",
        "batch.size",
        "compression"
      ],
      "answer": "**`linger.ms`** trades **latency** for **batching**; **`batch.size`** caps batch; **compression** reduces network—**measure** p99 vs MB/s.",
      "followUps": [
        {
          "question": "Out-of-order after retries?",
          "answer": "Without **idempotence**, retries can **duplicate** or **reorder** perception at broker—**EOS** patterns needed for exactly-once semantics."
        }
      ]
    },
    {
      "id": "d60-s3",
      "question": "Large messages in Kafka—concerns?",
      "signals": [
        "message.max.bytes",
        "heap",
        "anti-pattern"
      ],
      "answer": "Raises **broker/broker network** limits—often **object storage** + **reference** in message better—tune **max** carefully across **broker/producer/consumer**.",
      "followUps": [
        {
          "question": "Schema in message vs registry?",
          "answer": "**Registry** stores **schema** once; message carries **id**—smaller and **evolvable**."
        }
      ]
    }
  ]
}
