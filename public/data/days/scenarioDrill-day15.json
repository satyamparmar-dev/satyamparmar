{
  "day": 15,
  "title": "Iterators and Enhanced For Loop",
  "phaseId": "phase2",
  "tags": [
    "Iterator",
    "fail-fast",
    "for-each"
  ],
  "scenarios": [
    {
      "id": "d15-s1",
      "question": "`ConcurrentModificationException` during `for (String s : list) { if (cond) list.remove(s); }`—explain and fix.",
      "signals": [
        "fail-fast",
        "Iterator.remove",
        "removeIf",
        "CopyOnWrite"
      ],
      "answer": "## In simple terms\n**Enhanced for** uses an **iterator**. **Structural modification** while iterating (without using iterator’s API) invalidates it → **fail-fast** exception.\n\n## Fixes\n1. **`iterator.remove()`** while looping with explicit iterator.\n2. **`list.removeIf(predicate)`** (Java 8+).\n3. Collect **to-remove** in a second pass or **stream filter** to new list.\n4. **`CopyOnWriteArrayList`** if read-heavy and removals rare.\n\n## Interview tip\nContrast **fail-fast** (early detection) vs **weakly consistent** iterators in concurrent collections.",
      "followUps": [
        {
          "question": "Why not catch the exception and continue?",
          "answer": "**Wrong**—you’re fighting the contract; data may be **skipped** or **corrupted** logically even if sometimes it “works.”"
        },
        {
          "question": "Does for-each work on Map?",
          "answer": "Loop **`entrySet()`**, **`keySet()`**, or **`values()`**—each delegates to iterator rules for that collection."
        }
      ]
    },
    {
      "id": "d15-s2",
      "question": "What is the difference between `Iterable` and `Iterator`?",
      "signals": [
        "iterator()",
        "foreach",
        "single-pass"
      ],
      "answer": "## In simple terms\n**`Iterable`** = “can give me an **`Iterator`**” (`iterator()` method). **`Iterator`** = **current position** in a sequence with `hasNext`/`next` (and optional `remove`).\n\n## Enhanced for\nCompiler desugars to **`iterator()`** calls—so your type must be **`Iterable`** or array.\n\n## Interview depth\n`Spliterator` for streams—**parallel** splitting.",
      "followUps": [
        {
          "question": "Can you iterate twice?",
          "answer": "Call **`iterator()`** again for a **fresh** iterator—unless documentation says **one-shot** (some streams/sources)."
        },
        {
          "question": "ListIterator extra features?",
          "answer": "**Bidirectional** traversal, **`set`/`add`**, index—useful for **editing** `List` safely at cursor."
        }
      ]
    },
    {
      "id": "d15-s3",
      "question": "Design a custom `Iterable` for paginated API results—what do you expose and what pitfalls do you avoid?",
      "signals": [
        "lazy fetch",
        "close resources",
        "Spliterator"
      ],
      "answer": "## Approach\nImplement **`Iterable<T>`** returning an **`Iterator<T>`** that **fetches next page** when buffer exhausted.\n\n## Pitfalls\n- **Resource leaks** if not **closable**—often better as **`Stream`** with **try-with-resources** or explicit **`AutoCloseable`** wrapper.\n- **Retry/idempotency** for failing pages.\n- **Total count** unknown—document **unbounded** iteration risk.\n\n## Interview tip\nMention **`Stream`** + **`Spliterator`** as modern alternative for **lazy** pipelines.",
      "followUps": [
        {
          "question": "Why Stream over Iterator for IO?",
          "answer": "**try-with-resources** integrates cleanly; **short-circuit** ops can stop early; **parallel** optional—Iterator is manual."
        }
      ]
    }
  ]
}
