{
  "day": 29,
  "title": "Lambda and Functional Interfaces",
  "phaseId": "phase4",
  "tags": [
    "SAM",
    "closure",
    "method reference"
  ],
  "scenarios": [
    {
      "id": "d29-s1",
      "question": "Lambda captures loop variable `i`—compile error in classic for-loop—why?",
      "signals": [
        "effectively final",
        "capture"
      ],
      "answer": "Lambdas capture **values**, not mutable loop index unless **final copy** per iteration—use **`final int idx = i`** or **IntStream**.\n\n**Effectively final** rule prevents **racey** shared mutation.",
      "followUps": [
        {
          "question": "Serializable lambdas?",
          "answer": "Possible with **caution**—captures must be safe; often **avoid**—prefer **named static** classes for distributed jobs."
        }
      ]
    },
    {
      "id": "d29-s2",
      "question": "`Function` vs `UnaryOperator` vs `Supplier`—pick for parsing config string to object.",
      "signals": [
        "arity",
        "type params"
      ],
      "answer": "**`Function<String,Config>`**—input→output. **`Supplier`** no input. **`UnaryOperator`** same in/out type—wrong here unless type matches both ends.",
      "followUps": [
        {
          "question": "Method reference `String::length` type?",
          "answer": "**`Function<String,Integer>`** (instance method **bound** to parameter as receiver)."
        }
      ]
    },
    {
      "id": "d29-s3",
      "question": "Exception in lambda to `Runnable`—checked exception issue?",
      "signals": [
        "try/catch",
        "wrapper",
        "unchecked"
      ],
      "answer": "**Runnable** doesn’t declare checked—must **catch** inside or wrap in **`RuntimeException`** or use custom **functional interface** declaring throws.",
      "followUps": [
        {
          "question": "Why not sneaky throw?",
          "answer": "Hurts **readability** and **API contracts**—use in **framework** code sparingly with documentation."
        }
      ]
    },
    {
      "id": "d29-s4",
      "question": "Write a `Comparator` for `Person` by last name, then first name, using lambdas or `Comparator.comparing`.",
      "signals": [
        "thenComparing",
        "nullsFirst"
      ],
      "answer": "`Comparator.comparing(Person::getLastName).thenComparing(Person::getFirstName)` — add **`nullsFirst`** on keys if nulls possible.",
      "followUps": [
        {
          "question": "Reverse order?",
          "answer": "`.reversed()` on the whole chain or `Comparator.comparing(...).reversed()`."
        }
      ]
    },
    {
      "id": "d29-s5",
      "question": "`Function.compose` vs `andThen` — which runs first when you chain?",
      "signals": [
        "order",
        "composition"
      ],
      "answer": "`f.andThen(g)` → apply **f** then **g**. `f.compose(g)` → apply **g** first, then **f** (read as “f **after** g”).",
      "followUps": [
        {
          "question": "Same for Predicate?",
          "answer": "Predicate has `and` / `or` / `negate` — not compose/andThen like Function."
        }
      ]
    },
    {
      "id": "d29-s6",
      "question": "When is a method reference like `String::valueOf` a `Function` vs `Supplier`?",
      "signals": [
        "target typing",
        "arity"
      ],
      "answer": "**Target type** decides: assigned to `Function<Object,String>` it’s **unbound** instance-like; **`Supplier`** needs **zero-arg** — `String::new` can be `Supplier<StringBuilder>` with right context.",
      "followUps": [
        {
          "question": "`System.out::println` type?",
          "answer": "Often **`Consumer<String>`** when used as `forEach(System.out::println)`."
        }
      ]
    }
  ]
}
