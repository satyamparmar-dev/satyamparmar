{
  "day": 11,
  "title": "Abstract Classes and Interfaces",
  "phaseId": "phase2",
  "tags": [
    "interface",
    "abstract",
    "default methods"
  ],
  "scenarios": [
    {
      "id": "d11-s1",
      "question": "Default methods in two interfaces conflict; class also extends abstract superclass—who wins?",
      "signals": [
        "class wins",
        "diamond problem",
        "must override conflict"
      ],
      "answer": "## Rules (simplified)\n1. **Class method** (abstract or concrete) **wins** over interface default for same signature.\n2. If **two interfaces** provide conflicting defaults and no class method, the concrete class must **override** and choose (or delegate).\n\n## Why it matters\nJava 8+ allows **interface evolution** without breaking implementors—but you must understand **resolution** to avoid surprise.",
      "followUps": [
        {
          "question": "Can interfaces have `static` methods?",
          "answer": "**Yes** (Java 8+). They are **not inherited**—called with **`InterfaceName.method()`**. Useful for **factory/helper** methods tied to the type."
        },
        {
          "question": "Abstract class vs interface for template method pattern?",
          "answer": "**Abstract class** can hold **state** and non-public helpers; define **final template method** calling abstract primitives. Interfaces can approximate with **private** methods since Java 9."
        }
      ]
    },
    {
      "id": "d11-s2",
      "question": "Marker interface (e.g. `Serializable`)—still relevant?",
      "signals": [
        "metadata for JVM",
        "annotations overlap",
        "serialVersionUID"
      ],
      "answer": "**Serializable** is still a real **JVM contract** for Java serialization (with security caveats). Many “marker” roles moved to **annotations**, but understand **serialization**, **Cloneable** pitfalls, and **`serialVersionUID`** for compatibility.\n\nInterview: prefer **JSON/Protobuf** for service contracts; Java serialization mainly **legacy**.",
      "followUps": [
        {
          "question": "Why is Java serialization risky?",
          "answer": "Can deserialize **attack gadgets** if bytes are untrusted—**remote code execution** history. Sanitize sources, use **allowlists**, or avoid entirely."
        }
      ]
    },
    {
      "id": "d11-s3",
      "question": "When would you use a `sealed` interface?",
      "signals": [
        "exhaustive switch",
        "controlled extension",
        "domain modeling"
      ],
      "answer": "**Sealed** types limit **who can implement/extend**—compiler can enforce **exhaustive** `switch`/pattern matching. Great for **AST nodes**, **payment result** variants, **domain events** with finite kinds.\n\nCombine with **`permits`** clause listing allowed subclasses.",
      "followUps": [
        {
          "question": "Sealed class in same module requirement?",
          "answer": "Permitted subclasses usually must be **accessible** to the sealed class (same module/package rules apply)—shows you read JLS basics."
        }
      ]
    }
  ]
}
